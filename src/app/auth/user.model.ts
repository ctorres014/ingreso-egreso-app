export class User {
    // private nombre: string;
    // private email: string;
    // private uid: string;

    constructor(obj: DataObj) {
    }
}

interface DataObj {
    nombre: string;
    email: string;
    uid: string;
}
